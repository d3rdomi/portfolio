---
import BaseHead from '../../components/BaseHead.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import { getCollection } from 'astro:content';
import '../../styles/global-style.css';
import '../../styles/photography-style.css';
import MyFooter from '../../components/MyFooter.astro';
import Navbar from '../../components/Navbar.astro';
const posts = (await getCollection('photography')).sort(
	(a, b) => a.data.pubDate.valueOf() - b.data.pubDate.valueOf()
);


---

<!doctype html>
<html lang="de">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />	
		
	</head>
	<body>
		<Navbar class="navbar" />
		<div class="header">
			<h1>photography</h1>
		</div>

		<main>
			<section>
				<ul>
					{
						posts.map((post) => (
							<li>
								<a href={`/photography/${post.slug}/`}>
									{post.data.heroImage && <img src={post.data.heroImage} alt={post.data.altText ?? ''} />}

									<h2 class="title">{post.data.title}</h2>
								</a>
							</li>
						))
					}
				</ul>
			</section>
		</main>
		
		<MyFooter />

	</body>
</html>