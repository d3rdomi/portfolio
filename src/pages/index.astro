---
// Fallback: Die Middleware leitet "/" normalerweise schon nach Browsersprache um.
// Sollte sie umgangen werden, landet man hier und wird auf Deutsch geleitet.
import { defaultLang } from '../i18n/ui';
return Astro.redirect(`/${defaultLang}`, 302);
---
